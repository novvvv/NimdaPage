spring:
  application:
    name: nimda-con
  
  datasource:
    url: jdbc:mariadb://localhost:3306/nimda_con?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 0908
    driver-class-name: org.mariadb.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update # 기존 데이터 보존하면서 스키마 업데이트 
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true # sql Formatting
        show_sql: true 
  
  security:
    user:
      name: admin
      password: 1234

server:
  port: 3001

logging:
  level:
    com.nimda.con: DEBUG
    org.springframework.security: DEBUG
