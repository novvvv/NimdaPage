info:
  name: 게시글 좋아요 조회
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/like/board/{boardId}/likeCount
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
