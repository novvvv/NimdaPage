info:
  name: 게시글 좋아요 수와 좋아요 눌렀는지 조회
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/like/board/{boardId}/likeStatus
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
