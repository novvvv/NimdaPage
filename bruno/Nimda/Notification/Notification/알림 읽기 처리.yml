info:
  name: 알림 읽기 처리
  type: http
  seq: 4

http:
  method: PATCH
  url: /api/notifications/{notificationId}/read
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
