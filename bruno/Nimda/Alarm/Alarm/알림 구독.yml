info:
  name: 알림 구독
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/alarm//subscribe/{userId}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
