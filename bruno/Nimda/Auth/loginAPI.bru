meta {
  name: loginAPI
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "userId": "{{adminUserId}}",
    "password": "{{adminPassword}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
